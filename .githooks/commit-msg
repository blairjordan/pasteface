#!/bin/sh
subject=$(sed -n '1p' "$1")
if ! printf '%s\n' "$subject" | LC_ALL=C grep -Eq '^(feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert)(\([A-Za-z0-9._/-]+\))?!?: .+'; then
    printf '%s\n' 'Use a Conventional Commit, e.g. feat: add transcript tabs' >&2
    exit 1
fi
